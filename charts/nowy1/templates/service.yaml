apiVersion: v1
kind: Service
metadata:
  name: {{ include "nowy1.fullname" . }}
  labels:
    {{- include "nowy1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nowy1.selectorLabels" . | nindent 4 }}
